# ADR-008: Unix socket остаётся default transport для локального ingestion

[English version](../../../docs/06-architecture/adr/ADR-008.md)

- **Статус:** принято  
- **Дата:** 2026-07-28  
- **Владельцы решения:** сопровождающие MetricShell  
- **Связанное исследование:** `INV-008 — Local Push Ingestion`  
- **Доказательства:** [INV-008](../../../research/INV-008/report_ru.md)

## Контекст

MetricShell принимает application metrics локально внутри workload container. ADR-005 сохраняет Unix socket и file
ingestion как стабильные локальные механизмы. INV-008 проверяет, создаёт ли дополнительный HTTP или gRPC push API
достаточную integration или performance ценность, чтобы оправдать ещё один production protocol.

Кандидаты:

1. не добавлять push API и использовать framed Unix domain socket;
2. добавить local HTTP/1.1 JSON API;
3. добавить local unary gRPC/protobuf API.

Все кандидаты должны иметь одинаковую семантику: один request содержит `N` records, validation атомарна, accepted state
изменяется только после validation, decoded payload limits не зависят от transport, restart adapter не удаляет state,
которым владеет MetricShell.

TCP API также добавляет bind configuration, parser/protocol surface, client dependencies, schema versioning и риск
endpoint exposure.

## Решение

MetricShell **не требует дополнительный local HTTP или gRPC push API**.

Unix domain socket остаётся default event-oriented transport для локального ingestion.

### Опциональный HTTP compatibility adapter

Local HTTP JSON adapter может быть реализован только как опциональная compatibility feature для конкретной integration,
которая не может обоснованно использовать Unix socket protocol.

Adapter должен:

- bind к `127.0.0.1` по умолчанию;
- использовать versioned path `/v1/metrics`;
- применять общий decoded application limit;
- различать malformed syntax, semantic rejection и size rejection;
- ограничивать body reads и request deadlines;
- валидировать полный request до изменения state;
- сохранять MetricShell-owned state при stop/restart adapter;
- требовать отдельное security design для wildcard или external bind.

### gRPC

gRPC не входит в default или запланированный local ingestion surface.

Unary gRPC/protobuf быстрее JSON HTTP при concurrent batching, но медленнее Unix socket и добавляет protobuf schema
management, generated clients, HTTP/2 dependencies и повышенный adoption cost, в частности для PHP.

Пересмотр допустим только при конкретной typed-client или interoperability requirement и новых доказательствах.

### Исходные границы

- maximum decoded request: `1 MiB`;
- tested batch: `1–16` records;
- tested producer concurrency: `1–8`;
- malformed JSON: `400 Bad Request`;
- empty/semantic HTTP request: `422 Unprocessable Entity`;
- decoded/encoded HTTP oversize: `413 Payload Too Large`;
- gRPC decoded oversize: `ResourceExhausted`;
- invalid request не изменяет accepted state.

Это feasibility bounds, а не production capacity guarantees. Cardinality, allocations, queue и sustained-load limits
определяются INV-014 и INV-015.

## Доказательства

Оба прогона использовали fingerprint:

```text
34bee766d38ee43421cd100d3b23a387b7736c660d13bd6e28b591505bd101d4
```

| Environment           | Docker | Kernel           | Architecture | Result set         |
|-----------------------|--------|------------------|--------------|--------------------|
| Docker Desktop macOS  | 29.4.3 | LinuxKit 6.12.76 | aarch64      | `20260727T185055Z` |
| Docker Desktop Ubuntu | 27.4.0 | LinuxKit 6.10.14 | x86_64       | `20260728T114459Z` |

В обоих прогонах benchmark scope clean, untracked count равен нулю. В каждом environment:

- прошли 14/14 correctness assertions;
- 36/36 performance rows завершились без request errors;
- принято 385,313 records;
- все transports приняли decoded `1 MiB`;
- все transports отклонили `1 MiB + 1 byte`;
- HTTP status classes для malformed, empty и encoded oversize совпали с contract;
- прошли loopback bind, state retention и restart HTTP adapter.

Для `1 KiB`, batch 16 и восьми producers:

| Environment     | Unix records/s | HTTP records/s | gRPC records/s | Unix p95 |  HTTP p95 | gRPC p95 |
|-----------------|---------------:|---------------:|---------------:|---------:|----------:|---------:|
| macOS/LinuxKit  |      1,630,940 |         89,072 |        634,355 | 0.245 ms |  3.246 ms | 1.046 ms |
| Ubuntu/LinuxKit |        928,096 |         21,172 |        187,377 | 0.400 ms | 11.301 ms | 2.297 ms |

Timing distributions различаются, но ranking и portable correctness совпадают. Unix лидирует; gRPC быстрее JSON HTTP
при concurrent batching; оба TCP candidates уступают Unix.

## Последствия

### Положительные

- Сохраняется один эффективный default event-oriented protocol.
- Default image не требует HTTP/2, protobuf runtime и generated gRPC clients.
- Минимизируется local endpoint exposure.
- Batch semantics и decoded limits остаются transport-independent.
- Опциональный HTTP решает стандартные integration cases без превращения в архитектурное требование.

### Отрицательные

- Clients без Unix socket support требуют optional HTTP или другой supported mechanism.
- Unix clients используют MetricShell-specific framed protocol.
- Default transport ориентирован на Linux/container и не является remote API.
- Не используются typed generated-client model gRPC и его преимущество над JSON HTTP.

### Эксплуатационные

- Local ingestion endpoint не публикуется за пределы container network namespace по умолчанию.
- HTTP adapter должен диагностировать bind address, rejection class и payload limit.
- Loopback bind не является authentication для процессов в том же network namespace.
- Wildcard bind, TLS/mTLS и authentication требуют отдельного security decision.

## Отклонённые альтернативы

### Обязательный HTTP JSON

**Отклонено как default requirement.**

HTTP прост для integration/debugging, но дублирует Unix ingestion, медленнее в тестах и добавляет TCP listener и
JSON/base64 parser surface. Он остаётся optional compatibility adapter.

### Обязательный gRPC

**Отклонено.**

gRPC быстрее JSON HTTP при batch concurrency, но не превосходит Unix и имеет максимальную стоимость schema/toolchain и
client adoption.

### Wildcard HTTP/gRPC bind по умолчанию

**Отклонено.**

External exposure расширяет trust boundary и требует authentication, encryption, rate limiting и network policy.

### Transport-specific decoded limits

**Отклонено.**

Wire overhead различается. Application contract использует единый decoded limit; transport wire/body guard может быть
больше, чтобы представить допустимый decoded request.

## Границы решения

ADR не определяет:

- final metric wire schema;
- maximum series, labels или label bytes;
- queue depth и backpressure;
- streaming gRPC;
- remote ingestion;
- wildcard bind security, TLS/mTLS или authentication;
- native non-LinuxKit Linux performance;
- Kubernetes network behavior;
- final benchmark targets.

## Требования к реализации

- Unix batch framing: record count и per-record length;
- atomic validation до state mutation;
- transport-independent decoded payload accounting;
- boundary tests на limit и limit + 1 byte;
- malformed, empty и oversize tests;
- bounded allocation, body read и deadlines;
- loopback-only HTTP default;
- explicit API versioning;
- adapter shutdown/restart state retention tests;
- cross-architecture container integration tests.

## Ограничения

Оба evidence environments используют LinuxKit. Matching aarch64/x86_64 результаты подтверждают cross-architecture
behavior в macOS/LinuxKit и Ubuntu/LinuxKit, но не native non-LinuxKit Linux, containerd, CRI-O или Kubernetes.

Prototype считает synthetic records и не моделирует final parser, cardinality enforcement, conflict resolution,
persistence или production backpressure.

## Ссылки

- [ADR-005 — Модель ingestion transport](ADR-005.md)
- [ADR-006 — File ingestion](ADR-006.md)
- [Architecture Investigation](../../05-architecture-investigation/architecture-investigation.md)
- [Architecture Research](../../05-architecture-investigation/architecture-research.md#inv-008)
- [INV-008 README](../../../research/INV-008/README_ru.md)
- [INV-008 Report](../../../research/INV-008/report_ru.md)
- [Evidence macOS](../../../research/INV-008/results/20260727T185055Z/environment.tsv)
- [Evidence Ubuntu](../../../research/INV-008/results/20260728T114459Z/environment.tsv)
